# Prisma SQLite (local). On Vercel this is overridden to file:/tmp/annotate.db
DATABASE_URL="file:../db/custom.db"

# 1 = heuristic fallbacks only (required on Vercel unless you add LLM credentials)
SKIP_LLM=1
